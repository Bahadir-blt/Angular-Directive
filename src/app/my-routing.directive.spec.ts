import { MyRoutingDirective } from './my-routing.directive';

describe('MyRoutingDirective', () => {
  it('should create an instance', () => {
    const directive = new MyRoutingDirective();
    expect(directive).toBeTruthy();
  });
});
